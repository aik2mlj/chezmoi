# Sioyek user keybindings - matching zathura config

# ---------- SCROLLING (Matching zathura J/K for half-page scroll) ----------
# J/K for half-page (screen) scroll up/down
screen_down    J
screen_up    K

# ---------- PAGE NAVIGATION (Matching zathura Left/Right) ----------
# Left/Right arrow keys for previous/next page
previous_page    <left>
next_page    <right>

# ---------- HISTORY NAVIGATION (Matching zathura Alt+Left/Right for jumplist) ----------
# Alt+Left/Right for history navigation
prev_state    <A-<left>>
next_state    <A-<right>>

# ---------- BOOKMARKS (Matching zathura Ctrl+M and Ctrl+G) ----------
# Ctrl+M to add bookmark (similar to zathura :bmark)
add_bookmark    <C-m>

# Ctrl+G to open global bookmarks list (similar to zathura :blist)
goto_bookmark_g    <C-g>

# ---------- RELOAD (Matching zathura Ctrl+R) ----------
# Ctrl+R to reload
reload    <C-r>

# ---------- DUAL PAGE MODE ----------
# D to toggle two-page view (similar to zathura first-page-column 1:2)
# Note: sioyek doesn't have exact equivalent, using toggle_presentation_mode as alternative
# or we can use a custom command if needed

# ---------- FIT TO WIDTH ----------
# = for fit to page width (matches zathura default)
fit_to_page_width    <equal>

# ---------- DARK MODE / RECOLOR TOGGLE ----------
# F8 for dark mode toggle (similar to zathura recolor toggle)
toggle_dark_mode    <f8>
toggle_custom_color    <C-i>

# ---------- COPY TO CLIPBOARD ----------
# Copy is already <C-c> by default which is good

# ---------- TABLE OF CONTENTS ----------
# t for table of contents (matching zathura default)
goto_toc    t

# ---------- SEARCH ----------
# / for search (matching zathura)
search    /

# ---------- QUIT ----------
close_window q
