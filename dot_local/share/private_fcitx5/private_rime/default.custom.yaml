patch:
  # 仅使用「雾凇拼音」的默认配置，配置此行即可
  __include: rime_ice_suggestion:/
  schema_list:
    - schema: double_pinyin_flypy    # 小鹤双拼
    - schema: toki_pona
    - schema: sitelen_pona
  # 以下可根据自己所需进行自定义，仅做参考。
